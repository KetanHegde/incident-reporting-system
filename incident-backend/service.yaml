apiVersion: v1
kind: Service
metadata:
  name: incident-backend-service
spec:
  type: NodePort
  selector:
    app: incident-backend
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30007
